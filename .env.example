# Copy to ".env" and set your secret values. ".env" is gitignored.
#
# EmDash CLI (remote): https://docs.emdashcms.com/
EMDASH_TOKEN=

# Optional: override Workers origin for scripts/emdash-remote.sh
# EMDASH_REMOTE_ORIGIN=https://emdash-blog-repo.denshoch.workers.dev
